# API base URL (no trailing slash). CORS must be enabled on the API.
VITE_API_URL=http://localhost:3000

# Production (Vercel): full URL of the API deployment
# VITE_API_URL=https://your-api.vercel.app

# Optional dev proxy (no CORS): Vite rewrites /api → localhost:3000
# VITE_API_URL=/api
