# API base URL (no trailing slash). In dev you can use the Vite proxy instead.
VITE_API_URL=http://localhost:3000

# Set to /api to use Vite dev proxy (avoids CORS in development)
# VITE_API_URL=/api
